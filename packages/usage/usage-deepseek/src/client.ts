/**
 * Client-namespace projection of the usage domain: a pure re-export of the
 * package's types outlet, so Web client code imports the client namespace
 * (repo discipline) and reads the same single-source types the Host emits.
 * @module @deepseek-ai/dsh-usage-deepseek/client
 */

export type * from './types.ts'
